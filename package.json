{
  "name": "digital-servant",
  "version": "0.1.0-beta.1",
  "private": true,
  "description": "A local-first OpenCode desktop workspace for Windows and WSL.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  }
}
