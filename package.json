{
  "name": "seneschal-ai",
  "version": "0.2.1-beta.9",
  "private": true,
  "description": "Seneschal is a private, local-first OpenCode workspace for Windows and WSL.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  }
}
